/*global define*/

define([
    'jquery',
    'backbone'
], function ($, Backbone) {
    'use strict';

    var FontyRouter = Backbone.Router.extend({
        routes: {
        }

    });

    return FontyRouter;
});
